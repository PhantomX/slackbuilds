ortp|-----handy-ruler------------------------------------------------------|
ortp: ortp (A C library implementing the RTP protocol (RFC3550))
ortp:
ortp: oRTP is a C library that implements RTP (RFC3550).
ortp:
ortp: http://www.linphone.org/index.php/code_review/ortp
ortp:
ortp:
ortp: Packager: _PACKAGER on _BUILD_DATE
ortp:
ortp:
ortp:
