mod_dnssd|-----handy-ruler------------------------------------------------------|
mod_dnssd: mod_dnssd (An Apache HTTPD module which adds Zeroconf support)
mod_dnssd:
mod_dnssd: mod_dnssd is an Apache HTTPD module which adds Zeroconf support via
mod_dnssd: DNS-SD using Avahi.
mod_dnssd:
mod_dnssd: http://0pointer.de/lennart/projects/mod_dnssd/
mod_dnssd:
mod_dnssd:
mod_dnssd: Packager: _PACKAGER on _BUILD_DATE
mod_dnssd:
mod_dnssd:
