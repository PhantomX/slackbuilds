#
# Description: Add a shortcut for MenuBarAction )
# Forwarded: No (review in progress )
# Bug: N/A
# Author: nlecureuil@mandriva.com
#

Index: apps/konsole/src/MainWindow.cpp
===================================================================
--- apps/konsole/src/MainWindow.cpp	(révision 1119476)
+++ apps/konsole/src/MainWindow.cpp	(copie de travail)
@@ -267,5 +267,8 @@
     _toggleMenuBarAction = new KToggleAction(this);
     _toggleMenuBarAction->setText( i18n("Show Menu Bar") );
     _toggleMenuBarAction->setIcon( KIcon("show-menu") );
+    // Add a shortcut for the MenuBar hide 
+    // if not this is not possible to have the menubar back.
+    _toggleMenuBarAction->setShortcut( Qt::CTRL + Qt::SHIFT + Qt::Key_F6 );
     connect( _toggleMenuBarAction , SIGNAL(toggled(bool)) , menuBar() , SLOT(setVisible(bool)) );
     collection->addAction("show-menubar",_toggleMenuBarAction);
