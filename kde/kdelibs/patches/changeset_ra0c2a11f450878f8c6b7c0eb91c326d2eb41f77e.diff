commit a0c2a11f450878f8c6b7c0eb91c326d2eb41f77e
Author: Àlex Fiestas <afiestas@kde.org>
Date:   Tue Aug 27 14:36:09 2013 +0200

    Correcte the check for max_bits, so we avoid the overflow.
    
    BUG:323812
    FIXED-IN: 4.11.1

diff --git a/solid/solid/backends/udev/utils.cpp b/solid/solid/backends/udev/utils.cpp
index d924a55..d373169 100644
--- a/solid/solid/backends/udev/utils.cpp
+++ b/solid/solid/backends/udev/utils.cpp
@@ -30,7 +30,7 @@ int Solid::Backends::UDev::input_str_to_bitmask(const QByteArray& value, long in
     memset (bitmask, 0, max_size);
     QList<QByteArray> bits = value.split(' ');
     for (i = bits.length() - 1, j = 0; i >= 0; i--, j++) {
-        if (j > max_bits) {
+        if (j >= max_bits) {
             qWarning() << "Solid::Backends::UDev::input_str_to_bitmask can't handle some bits" << bits;
             return num_bits_set;
         }
