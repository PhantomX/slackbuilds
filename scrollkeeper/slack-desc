            |-----handy-ruler------------------------------------------------------|
scrollkeeper: scrollkeeper (A cataloging system for documentation on open systems)
scrollkeeper:
scrollkeeper: ScrollKeeper is a cataloging system for documentation. It manages
scrollkeeper: documentation metadata (as specified by the Open Source Metadata
scrollkeeper: Framework (OMF)) and provides a simple API to allow help browsers to
scrollkeeper: find, sort, and search the document catalog. It can also communicate
scrollkeeper: with catalog servers on the Net to search for documents which are not
scrollkeeper: on the local system.
scrollkeeper:
scrollkeeper: ftp://ftp.gnome.org
scrollkeeper: Packager: _PACKAGER on _BUILD_DATE
