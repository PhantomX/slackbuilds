cloog|-----handy-ruler------------------------------------------------------|
cloog: cloog (The Chunky Loop Generator)
cloog:
cloog: CLooG is a software which generates loops for scanning Z-polyhedra.
cloog: That is, CLooG finds the code or pseudo-code where each integral point
cloog: of one or more parametrized polyhedron or parametrized polyhedra union
cloog: is reached. CLooG is designed to avoid control overhead and to produce
cloog: a very efficient code.
cloog:
cloog: http://www.cloog.org
cloog:
cloog: Packager: _PACKAGER on _BUILD_DATE
