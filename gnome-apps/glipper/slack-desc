       |-----handy-ruler------------------------------------------------------|
glipper: glipper (Glipper is a clipboardmanager for GNOME)
glipper:
glipper: Glipper is a clipboardmanager for GNOME (and other WMs). You can see
glipper: this as a GNOME counterpart to KDE's Klipper. But it only depends on
glipper: GTK+ 2 and NOT on GNOME libs, so you can also use it with any other
glipper: window manager that supports tray icons.
glipper:
glipper: Project website: http://sourceforge.net/projects/glipper/
glipper:
glipper:
glipper: Packager: _PACKAGER on _BUILD_DATE
