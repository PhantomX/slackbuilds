--- src/ffaudio/ffaudio-core.c.ffmpeg	2011-05-19 22:51:09.077105613 -0300
+++ src/ffaudio/ffaudio-core.c	2011-05-19 22:51:29.765706402 -0300
@@ -341,7 +341,7 @@ static Tuple * read_tuple (const gchar *
     {
         s = ic->streams[i];
         c = s->codec;
-        if (c->codec_type == CODEC_TYPE_AUDIO)
+        if (c->codec_type == AVMEDIA_TYPE_AUDIO)
         {
             av_find_stream_info(ic);
             codec = avcodec_find_decoder(c->codec_id);
@@ -415,7 +415,7 @@ static gboolean ffaudio_play (InputPlayb
     {
         s = ic->streams[i];
         c = s->codec;
-        if (c->codec_type == CODEC_TYPE_AUDIO)
+        if (c->codec_type == AVMEDIA_TYPE_AUDIO)
         {
             av_find_stream_info(ic);
             codec = avcodec_find_decoder(c->codec_id);
