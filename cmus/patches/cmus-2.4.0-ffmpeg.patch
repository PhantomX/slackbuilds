--- ffmpeg.c.ffmpeg	2011-05-20 18:46:14.713145808 -0300
+++ ffmpeg.c	2011-05-20 18:46:28.021322099 -0300
@@ -183,7 +183,7 @@ static int ffmpeg_open(struct input_plug
 
 		for (i = 0; i < ic->nb_streams; i++) {
 			cc = ic->streams[i]->codec;
-			if (cc->codec_type == CODEC_TYPE_AUDIO) {
+			if (cc->codec_type == AVMEDIA_TYPE_AUDIO) {
 				stream_index = i;
 				break;
 			}
