Author: Loic Minier <lool@dooz.org>
Description: Unbreak several useful keybindings.
--- a/termcap.c
+++ b/termcap.c
@@ -552,8 +552,6 @@
       else
 	break;
     }
-  if (n < KMAP_KEYS)
-    domap = 1;
   if (map == 0 && domap)
     return 0;
   if (map && !domap)
