numactl|-----handy-ruler------------------------------------------------------|
numactl: numactl (Library for tuning for Non Uniform Memory Access machines)
numactl:
numactl: Simple NUMA policy support. It consists of a numactl program to run
numactl: other programs with a specific NUMA policy and a libnuma to do
numactl: allocations with NUMA policy in applications.
numactl:
numactl: ftp://oss.sgi.com/www/projects/libnuma/download
numactl:
numactl:
numactl: Packager: _PACKAGER on _BUILD_DATE
numactl:
