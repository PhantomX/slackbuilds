    |-----handy-ruler------------------------------------------------------|
qemu: qemu (QEMU is a FAST! processor emulator)
qemu:
qemu: By using dynamic translation it achieves a reasonable speed while
qemu: being easy to port on new host CPUs. QEMU has two operating modes:
qemu: - User mode emulation.
qemu: - Full system emulation.
qemu: As QEMU requires no host kernel patches to run, it is very safe and
qemu: easy to use.
qemu: http://fabrice.bellard.free.fr/qemu/
qemu:
qemu: Packager: _PACKAGER on _BUILD_DATE
