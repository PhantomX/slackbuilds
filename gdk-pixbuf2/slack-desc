gdk-pixbuf2|-----handy-ruler------------------------------------------------------|
gdk-pixbuf2: gdk-pixbuf2 (An image loading library)
gdk-pixbuf2:
gdk-pixbuf2: gdk-pixbuf is an image loading library that can be extended by
gdk-pixbuf2: loadable modules for new image formats.
gdk-pixbuf2:
gdk-pixbuf2: http://www.gtk.org/
gdk-pixbuf2:
gdk-pixbuf2:
gdk-pixbuf2: Packager: _PACKAGER on _BUILD_DATE
gdk-pixbuf2:
gdk-pixbuf2:
