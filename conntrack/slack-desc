conntrack|-----handy-ruler------------------------------------------------------|
conntrack: conntrack (Tool to manipulate netfilter connection tracking table)
conntrack:
conntrack: conntrack is used to search, list, inspect and maintain the netfilter
conntrack: netfilter connection tracking subsystem of the Linux kernel. Using
conntrack: conntrack , you can dump a list of all (or a filtered selection of)
conntrack: currently tracked connections, delete connections from the state
conntrack: table, and even add new ones.
conntrack: In  addition, you can also monitor connection tracking events, e.g.
conntrack: show an event message (one line) per newly established connection.
conntrack: http://netfilter.org
conntrack: Packager: _PACKAGER on _BUILD_DATE
