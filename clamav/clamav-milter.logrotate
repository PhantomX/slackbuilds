/var/log/clamd {
    monthly
    notifempty

   postrotate
        test -x /etc/rc.d/rc.tor && /etc/rc.d/rc.clamav restart >/dev/null 2>/dev/null || :
   endscript
}

/var/log/clamav-update {
    monthly
    notifempty
    missingok
}
