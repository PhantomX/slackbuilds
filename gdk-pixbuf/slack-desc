gdk-pixbuf|-----handy-ruler------------------------------------------------------|
gdk-pixbuf: gdk-pixbuf (An image loading library used with GNOME)
gdk-pixbuf:
gdk-pixbuf: The gdk-pixbuf package contains an image loading library used with the
gdk-pixbuf: GNOME GUI desktop environment. The GdkPixBuf library provides image
gdk-pixbuf: loading facilities, the rendering of a GdkPixBuf into various formats
gdk-pixbuf: (drawables or GdkRGB buffers), and a cache interface.
gdk-pixbuf:
gdk-pixbuf: http://developer.gnome.org/arch/imaging/gdkpixbuf.html
gdk-pixbuf:
gdk-pixbuf:
gdk-pixbuf: Packager: _PACKAGER on _BUILD_DATE
