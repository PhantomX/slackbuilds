    |-----handy-ruler------------------------------------------------------|
fuse: fuse (File System in Userspace (FUSE) utilities)
fuse:
fuse: With FUSE it is possible to implement a fully functional filesystem
fuse: in a userspace program. This package contains the FUSE userspace
fuse: tools to mount a FUSE filesystem.
fuse:
fuse: Note: For security reasons only members of the group "fuse" are
fuse: allowed to (u)mount fuse filesystems. You can find more details on
fuse: this issue in /usr/doc/fuse-${VERSION}/README.fedora
fuse:
fuse: Packager: _PACKAGER on _BUILD_DATE
