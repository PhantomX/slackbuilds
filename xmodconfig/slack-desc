xmodconfig|-----handy-ruler------------------------------------------------------|
xmodconfig: xmodconfig (A graphical front end for xmodmap)
xmodconfig:
xmodconfig: xmodconfig is a graphical front end for xmodmap. It enables you to
xmodconfig: configure xmodmap files without using a texteditor or any additional
xmodconfig: application such as xev, so you can configure your key mapping without
xmodconfig: any knowledge of the syntax of xmodmap.
xmodconfig:
xmodconfig: http://sourceforge.net/projects/xmodconfig/
xmodconfig:
xmodconfig:
xmodconfig: Packager: _PACKAGER on _BUILD_DATE
