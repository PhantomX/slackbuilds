commit f40d7a41e78bf229e2a7fa7d921e77157dc01ac8
Author: Thomas Lübking <thomas.luebking@gmail.com>
Date:   Fri Mar 1 21:08:16 2013 +0100

    Only prefer current tabgroup if it's shown
    
    BUG: 315956
    FIXED-IN:4.10.2

diff --git a/kwin/activation.cpp b/kwin/activation.cpp
index a3c050c..8646f48 100644
--- a/kwin/activation.cpp
+++ b/kwin/activation.cpp
@@ -458,7 +458,7 @@ bool Workspace::activateNextClient(Client* c)
     Client* get_focus = NULL;
 
     // precedence on keeping the current tabgroup active. to the user that's the same window
-    if (c && c->tabGroup()) {
+    if (c && c->tabGroup() && c->isShown(false)) {
         if (c == c->tabGroup()->current())
             c->tabGroup()->activateNext();
         get_focus = c->tabGroup()->current();
