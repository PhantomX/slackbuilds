--- decoder_plugins/ffmpeg/ffmpeg.c.ffmpeg	2011-05-20 22:27:58.250173023 -0300
+++ decoder_plugins/ffmpeg/ffmpeg.c	2011-05-20 22:28:14.381803209 -0300
@@ -137,7 +137,7 @@ static void *ffmpeg_open (const char *fi
 	av_read_play (data->ic);
 	for (i = 0; i < data->ic->nb_streams; i++) {
 		data->enc = data->ic->streams[i]->codec;
-		if (data->enc->codec_type == CODEC_TYPE_AUDIO) {
+		if (data->enc->codec_type == AVMEDIA_TYPE_AUDIO) {
 			audio_index = i;
 			break;
 		}
