           |-----handy-ruler-------------------------------------------------------|
rox-archive: rox-archive (Archive manager for ROX)
rox-archive:
rox-archive: You can use this program to compress a directory or file into a single,
rox-archive: smaller file (which can be stored, emailed, etc).
rox-archive: You can also use it to extract a compressed archive back into a file
rox-archive: or directory structure.
rox-archive:
rox-archive: http://rox.sourceforge.net
rox-archive:
rox-archive:
rox-archive: Packager: _PACKAGER on _BUILD_DATE
