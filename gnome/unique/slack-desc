unique|-----handy-ruler------------------------------------------------------|
unique: unique (Unique instance support for applications)
unique:
unique: Unique is a library for writing single instance applications, that is
unique: applications that are run once and every further call to the same
unique: binary either exits immediately or sends a command to the running
unique: instance.
unique:
unique: http://www.gnome.org/~ebassi/source/
unique:
unique:
unique: Packager: _PACKAGER on _BUILD_DATE
