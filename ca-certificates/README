Add extra certificates in ${CWD}/extra if needed, as X.509 .crt, without blank spaces 
and LF line terminations.
Remember this can have security issues!

openssl pkcs7 -inform DER -in cert.p7b -print_certs -out cert.pem
