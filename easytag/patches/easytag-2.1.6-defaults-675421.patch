https://bugzilla.redhat.com/show_bug.cgi?id=675421

diff -up easytag-2.1.6/src/about.c~ easytag-2.1.6/src/about.c
--- easytag-2.1.6/src/about.c~	2008-03-12 23:24:40.000000000 +0200
+++ easytag-2.1.6/src/about.c	2011-02-05 16:16:45.394207722 +0200
@@ -699,6 +699,7 @@ void About_Window_Go_To_Home_Page (void)
 #ifdef WIN32
     wineasytag_notify_uri(WEBPAGE);
 #else
+    if (system("xdg-open "WEBPAGE)!=0)
     if (system("gnome-moz-remote "WEBPAGE)!=0)
         if (system("x-www-browser "WEBPAGE)!=0)
             system("www-browser "WEBPAGE);
diff -up easytag-2.1.6/src/setting.c~ easytag-2.1.6/src/setting.c
--- easytag-2.1.6/src/setting.c~	2008-05-25 01:32:38.000000000 +0300
+++ easytag-2.1.6/src/setting.c	2011-02-05 16:46:32.468290939 +0200
@@ -339,12 +339,14 @@ tConfigVariable Config_Variables[] =
  */
 void Init_Config_Variables (void)
 {
+    gchar *defpath = g_get_user_special_dir(G_USER_DIRECTORY_MUSIC);
+    if (!defpath) defpath = HOME_VARIABLE;
 
     /*
      * Common
      */
     LOAD_ON_STARTUP               = 0;
-    DEFAULT_PATH_TO_MP3           = g_strdup(HOME_VARIABLE);
+    DEFAULT_PATH_TO_MP3           = g_strdup(defpath);
     BROWSE_SUBDIR                 = 1;
 #ifdef WIN32
     BROWSE_HIDDEN_DIR             = 1;
@@ -392,7 +394,7 @@ void Init_Config_Variables (void)
 #ifdef WIN32
     AUDIO_FILE_PLAYER                       = ET_Win32_Get_Audio_File_Player();
 #else
-    AUDIO_FILE_PLAYER                       = g_strdup("xmms -p");
+    AUDIO_FILE_PLAYER                       = g_strdup("xdg-open");
 #endif
 
     /*
