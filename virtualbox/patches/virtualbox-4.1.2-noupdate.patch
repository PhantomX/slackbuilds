diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/Makefile.kmk.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/Makefile.kmk
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/Makefile.kmk.noup	2011-08-15 09:28:42.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/Makefile.kmk	2011-08-16 18:52:53.047866706 -0300
@@ -38,6 +38,8 @@ ifdef VBOX_WITH_REGISTRATION_REQUEST
  VBOX_WITH_REGISTRATION_REQUEST=
 endif
 
+# VBOX_WITH_UPDATE := 1
+
 # Show the update notifier dialog during startup. If this setting is disabled
 # the user can still update using the menu if desired.
 ifndef VBOX_OSE
@@ -121,6 +123,7 @@ endif
 VirtualBox_DEFS          += \
 	$(if $(VBOX_WITH_REGISTRATION),VBOX_WITH_REGISTRATION) \
 	$(if $(VBOX_WITH_REGISTRATION_REQUEST),VBOX_WITH_REGISTRATION_REQUEST) \
+	$(if $(VBOX_WITH_UPDATE),VBOX_WITH_UPDATE) \
 	$(if $(VBOX_WITH_UPDATE_REQUEST),VBOX_WITH_UPDATE_REQUEST) \
 	$(if $(VBOX_WITH_ALSA),VBOX_WITH_ALSA) \
 	$(if $(VBOX_WITH_PULSE),VBOX_WITH_PULSE) \
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.cpp.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.cpp
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.cpp.noup	2011-08-15 09:28:38.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.cpp	2011-08-16 18:52:53.048866492 -0300
@@ -296,6 +296,7 @@ protected:
 };
 #endif /* VBOX_WITH_REGISTRATION */
 
+#ifdef VBOX_WITH_UPDATE
 class PerformUpdateAction : public UISimpleAction
 {
     Q_OBJECT;
@@ -326,6 +327,7 @@ protected:
         setStatusTip(QApplication::translate("UIMessageCenter", "Check for a new VirtualBox version"));
     }
 };
+#endif /* VBOX_WITH_UPDATE */
 
 class ShowAboutAction : public UISimpleAction
 {
@@ -438,7 +440,9 @@ void UIActionPool::createActions()
 #ifdef VBOX_WITH_REGISTRATION
     m_pool[UIActionIndex_Simple_Register] = new PerformRegisterAction(this);
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
     m_pool[UIActionIndex_Simple_Update] = new PerformUpdateAction(this);
+#endif /* VBOX_WITH_UPDATE */
     m_pool[UIActionIndex_Simple_About] = new ShowAboutAction(this);
 }
 
@@ -464,6 +468,7 @@ void UIActionPool::createMenus()
         delete m_pool[UIActionIndex_Simple_Register]
     m_pool[UIActionIndex_Simple_Register] = new PerformRegisterAction(this);
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
 #if defined(Q_WS_MAC) && (QT_VERSION >= 0x040700)
     /* For whatever reason, Qt doesn't fully remove items with a
      * ApplicationSpecificRole from the application menu. Although the QAction
@@ -471,10 +476,13 @@ void UIActionPool::createMenus()
      * Hiding before deletion helps. */
     m_pool[UIActionIndex_Simple_Update]->setVisible(false);
 #endif
+#endif /* VBOX_WITH_UPDATE */
 #if !(defined(Q_WS_MAC) && (QT_VERSION < 0x040700))
+#ifdef VBOX_WITH_UPDATE
     if (m_pool[UIActionIndex_Simple_Update])
         delete m_pool[UIActionIndex_Simple_Update];
     m_pool[UIActionIndex_Simple_Update] = new PerformUpdateAction(this);
+#endif /* VBOX_WITH_UPDATE */
     if (m_pool[UIActionIndex_Simple_About])
         delete m_pool[UIActionIndex_Simple_About];
     m_pool[UIActionIndex_Simple_About] = new ShowAboutAction(this);
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.h.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.h
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.h.noup	2011-08-15 09:28:38.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/globals/UIActionPool.h	2011-08-16 18:52:53.048866492 -0300
@@ -45,7 +45,9 @@ enum UIActionIndex
 #ifdef VBOX_WITH_REGISTRATION
     UIActionIndex_Simple_Register,
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
     UIActionIndex_Simple_Update,
+#endif /* VBOX_WITH_UPDATE */
     UIActionIndex_Simple_About,
 
     /* Maximum index: */
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.cpp.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.cpp
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.cpp.noup	2011-04-28 05:01:00.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.cpp	2011-08-16 18:52:53.052865639 -0300
@@ -59,7 +59,9 @@ UIMachineShortcuts::UIMachineShortcuts()
 #ifdef VBOX_WITH_REGISTRATION
     m_Shortcuts[RegisterShortcut]              = UIKeySequence("Register");
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
     m_Shortcuts[UpdateShortcut]                = UIKeySequence("Update");
+#endif /* VBOX_WITH_UPDATE */
     m_Shortcuts[AboutShortcut]                 = UIKeySequence("About");
     m_Shortcuts[PopupMenuShortcut]             = UIKeySequence("PopupMenu",             "Home");
     /* Overwrite the key sequences with the one in extra data. */
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.h.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.h
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.h.noup	2011-04-28 05:01:00.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/runtime/UIMachineShortcuts.h	2011-08-16 18:52:53.052865639 -0300
@@ -57,7 +57,9 @@ public:
 #ifdef VBOX_WITH_REGISTRATION
         RegisterShortcut,
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
         UpdateShortcut,
+#endif /* VBOX_WITH_UPDATE */
         AboutShortcut,
         PopupMenuShortcut,
         EndShortcutType
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.cpp.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.cpp
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.cpp.noup	2011-07-14 14:03:32.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.cpp	2011-08-16 18:52:53.052865639 -0300
@@ -48,7 +48,9 @@ UISelectorShortcuts::UISelectorShortcuts
 #ifdef VBOX_WITH_REGISTRATION
     m_Shortcuts[RegisterShortcut]            = UIKeySequence("Register");
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
     m_Shortcuts[UpdateShortcut]              = UIKeySequence("Update");
+#endif /* VBOX_WITH_UPDATE */
     m_Shortcuts[AboutShortcut]               = UIKeySequence("About");
     /* Get a list of overwritten keys */
     loadExtraData(VBoxDefs::GUI_Input_SelectorShortcuts, EndShortcutType);
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.h.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.h
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.h.noup	2011-06-15 15:52:49.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/UISelectorShortcuts.h	2011-08-16 18:52:53.052865639 -0300
@@ -50,7 +50,9 @@ public:
 #ifdef VBOX_WITH_REGISTRATION
         RegisterShortcut,
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
         UpdateShortcut,
+#endif /* VBOX_WITH_UPDATE */
         AboutShortcut,
         EndShortcutType
     };
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/VBoxSelectorWnd.cpp.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/VBoxSelectorWnd.cpp
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/VBoxSelectorWnd.cpp.noup	2011-08-15 09:28:35.000000000 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/selector/VBoxSelectorWnd.cpp	2011-08-16 18:52:53.053865425 -0300
@@ -1751,7 +1751,9 @@ void VBoxSelectorWnd::prepareMenuHelp(QM
     pMenu->addAction(gActionPool->action(UIActionIndex_Simple_Register));
 #endif /* VBOX_WITH_REGISTRATION */
 
+#ifdef VBOX_WITH_UPDATE
     pMenu->addAction(gActionPool->action(UIActionIndex_Simple_Update));
+#endif /* VBOX_WITH_UPDATE */
 
 #ifndef Q_WS_MAC
     pMenu->addSeparator();
@@ -1772,8 +1774,10 @@ void VBoxSelectorWnd::prepareMenuHelp(QM
     VBoxGlobal::connect(gEDataEvents, SIGNAL(sigCanShowRegistrationDlg(bool)),
                         gActionPool->action(UIActionIndex_Simple_Register), SLOT(setEnabled(bool)));
 #endif /* VBOX_WITH_REGISTRATION */
+#ifdef VBOX_WITH_UPDATE
     VBoxGlobal::connect(gActionPool->action(UIActionIndex_Simple_Update), SIGNAL(triggered()),
                         gUpdateManager, SLOT(sltForceCheck()));
+#endif /* VBOX_WITH_UPDATE */
     VBoxGlobal::connect(gActionPool->action(UIActionIndex_Simple_About), SIGNAL(triggered()),
                         &msgCenter(), SLOT(sltShowHelpAboutDialog()));
 }
diff -up VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/settings/UISettingsDialogSpecific.cpp.noup VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/settings/UISettingsDialogSpecific.cpp
--- VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/settings/UISettingsDialogSpecific.cpp.noup	2011-08-16 19:02:23.162781062 -0300
+++ VirtualBox-4.1.2_OSE/src/VBox/Frontends/VirtualBox/src/settings/UISettingsDialogSpecific.cpp	2011-08-16 19:02:29.255174659 -0300
@@ -340,6 +340,7 @@ UISettingsDialogGlobal::UISettingsDialog
                             iPageIndex, "#input", pSettingsPage);
                     break;
                 }
+#ifdef VBOX_WITH_UPDATE
                 /* Update page: */
                 case GLSettingsPage_Update:
                 {
@@ -349,6 +350,7 @@ UISettingsDialogGlobal::UISettingsDialog
                             iPageIndex, "#update", pSettingsPage);
                     break;
                 }
+#endif /* VBOX_WITH_UPDATE */
                 /* Language page: */
                 case GLSettingsPage_Language:
                 {
@@ -475,9 +477,10 @@ void UISettingsDialogGlobal::retranslate
     /* Input page: */
     m_pSelector->setItemText(GLSettingsPage_Input, tr("Input"));
 
+#ifdef VBOX_WITH_UPDATE
     /* Update page: */
     m_pSelector->setItemText(GLSettingsPage_Update, tr("Update"));
-
+#endif /* VBOX_WITH_UPDATE */
     /* Language page: */
     m_pSelector->setItemText(GLSettingsPage_Language, tr("Language"));
 
