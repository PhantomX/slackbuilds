    |-----handy-ruler------------------------------------------------------|
jack: jack (The Jack Audio Connection Kit)
jack:
jack: JACK is a low-latency audio server, written primarily for the Linux
jack: operating system. It can connect a number of different applications
jack: to an audio device, as well as allowing them to share audio between
jack: themselves. Its clients can run in their own processes (ie. as a
jack: normal application), or can they can run within a JACK server (ie.
jack: a "plugin").
jack:
jack: http://www.jackaudio.org
jack: Packager: _PACKAGER on _BUILD_DATE
