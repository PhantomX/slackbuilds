kdesvn|-----handy-ruler------------------------------------------------------|
kdesvn: kdesvn (A subversion client for KDE4 with KIO integration)
kdesvn:
kdesvn: KDESvn is a frontend to the subversion vcs. In difference to most
kdesvn: other tools it uses the subversion C-Api direct via a c++ wrapper made
kdesvn: by Rapid SVN and doesn't parse the output of the subversion client. So
kdesvn: it is a real client itself instead of a frontend to the command line
kdesvn: tool.
kdesvn:
kdesvn: http://kdesvn.alwins-world.de/
kdesvn:
kdesvn: Packager: _PACKAGER on _BUILD_DATE
