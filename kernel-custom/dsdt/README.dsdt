To add a custom DSDT, place DSDT-${KERNNAME}.dsl.gz and DSDT-${KERNNAME}.hex.gz
(gzipped) on directory where this README resides.

SlackBuild will ignore if nothing found, SB_DSDT=NO or if set on kernel config.

Default ${KERNNAME} is "custom", see SlackBuild.
