libldb|-----handy-ruler------------------------------------------------------|
libldb: libldb (A schema-less, ldap like, API and database)
libldb:
libldb: An extensible library that implements an LDAP like API to access
libldb: remote LDAP servers, or use local tdb databases.
libldb:
libldb: http://ldb.samba.org/
libldb:
libldb:
libldb: Packager: _PACKAGER on _BUILD_DATE
libldb:
libldb:
