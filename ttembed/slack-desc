ttembed|-----handy-ruler------------------------------------------------------|
ttembed: ttembed (Remove embedding limitations from TrueType fonts)
ttembed:
ttembed: Remove embedding limitations from TrueType fonts, by setting the
ttembed: fsType field in the OS/2 table to zero. That's it; this program is a
ttembed: one-trick pony.
ttembed:
ttembed: https://github.com/hisdeedsaredust/ttembed
ttembed:
ttembed:
ttembed: Packager: _PACKAGER on _BUILD_DATE
ttembed:
