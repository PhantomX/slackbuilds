       |-----handy-ruler------------------------------------------------------|
md5deep: md5deep (A suite of tools to compute hashes)
md5deep:
md5deep: Md5deep is a suite of tools to compute hashes, or message digests for
md5deep: any number of files while optionally recursively digging through the
md5deep: directory structure.  It can also take a list of known hashes and
md5deep: display the filenames of input files whose hashes either do or do not
md5deep: match any of the known hashes. Errors are reported to standard error.
md5deep: If no FILES are specified, reads from standard input.
md5deep:
md5deep: http://md5deep.sourceforge.net/
md5deep: Packager: _PACKAGER on _BUILD_DATE
